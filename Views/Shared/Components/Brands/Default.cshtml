@model IEnumerable<BrandModel>

@foreach (var item in Model)
{
	<li><a asp-action="Index" asp-controller="Brand" asp-route-Slug="@item.Slug" > <span class="pull-right">(50)</span>@item.Name</a></li>
}